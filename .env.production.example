POSTGRES_DB=rigintel
POSTGRES_USER=rigintel
POSTGRES_PASSWORD=CHANGE_ME_POSTGRES

WP_DB_NAME=wordpress
WP_DB_USER=wordpress
WP_DB_PASSWORD=CHANGE_ME_WP
WP_DB_ROOT_PASSWORD=CHANGE_ME_WP_ROOT

SERVER_PORT=8080
ENVIRONMENT=production
PUBLIC_APP_URL=https://rigintel.ai/app
CORS_ORIGINS=https://rigintel.ai,https://www.rigintel.ai
NEXT_PUBLIC_SITE_URL=https://rigintel.ai

CERTBOT_EMAIL=admin@rigintel.ai
JWT_SECRET=CHANGE_ME_JWT
TOTP_KEY=CHANGE_ME_TOTP
SUPERADMIN_EMAIL=erman.ai@yandex.ru
SUPERADMIN_PASSWORD=CHANGE_ME_SUPERADMIN
SUPERADMIN_NAME=George
# EXTRACT_URL and EXTRACT_TOKEN default in Compose (JWT_SECRET).
# GATEWAY_URL, GATEWAY_TOKEN, STUDIO_URL default in Compose.
# Optional: UNSLOTH_PASSWORD (defaults to JWT_SECRET). Used for Studio login and first-boot seed.
# Do not set UNSLOTH_STUDIO_PASSWORD in .env — Compose seeds it only until auth.db exists on the work volume.
# VLLM_URL for chat proxy.

